{% set langRootUrl = '/' + lang if lang !== defaultLang else '' %}

<ul class="nav">

  {% include "layout/main-menu-main-pages.njk" %}

  {# Extra pages menu (dev-mode only) #}
  {% if site.showPagesMenu %}
  <!-- All pages dropdown tableau -->
  <li class="yamm megamenu le-dropdown">
    <ul class="nav">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          {{ 'headerLinks.pages' | i18n }}
        </a>
        <div class="dropdown-menu">
            {% include "layout/main-menu-extra-content.njk" %}
        </div>
      </li>
    </ul>
  </li>
  {% endif %}
</ul>
